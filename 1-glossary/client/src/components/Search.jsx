import React from 'react';

const Search = (props) => {
  return (
    <div id="search">
      <form>
        <input placeholder="search..." />
        <button>Search Now!</button>
      </form>
    </div>
  );
}

export default Search;